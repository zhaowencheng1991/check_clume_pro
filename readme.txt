#####

#version:v0.0.0
#2017-12-15
#^_^

简述:
简单的一个python脚本,应用于flume监控,会检测flume进程是否存在,不存在则报警。

在flume进程存在的情况下会把实际的日志大小与flume读取的日志大小做对比,如果差距超过预设的阀值则会报警,阀值可以在配置文件中设置,单位为B。

需要说明的是报警模块使用的是我司的接口,当然可以修改该(tool.py)。

